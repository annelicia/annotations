export enum ToolsType {
    RESIZE,
    MOVE,
    SELECT,
    TEXT,
    CIRCLE_FRAME,
    CIRCLE,
    TRIANGLE_FRAME,
    TRIANGLE,
    RECT_FRAME,
    RECT,
    LINE,
    PEN,
}

export enum ColorsType {
    BLACK = "black",
    RED = "red",
    YELLOW = "yellow",
    GREEN = "green",
    BLUE = "blue",
}

export enum WidthsPen {
    SMALL = "Small",
    MEDIUM = "Medium",
    BIG = "Big",
}
